import React from "react";

const WeatherTotal = ({ iconData, temp, pm10, pm25, o3 }) => {
  console.log(iconData);
  console.log(temp);
  console.log(pm10);
  console.log(pm25);

  return <></>;
};

export default WeatherTotal;
